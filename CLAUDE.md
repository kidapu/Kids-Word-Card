# おしゃべりカード（こども英語カードアプリ）

## これは何か

未就学児向けの、絵カードをタップすると英語と日本語の発音が鳴るアプリ。
市販の「音の出る英語カード」玩具のデジタル版。iPad / iPhone のホーム画面から
PWA として起動して使うことを想定している。

作者の子どもが実際に使う。想定利用シーンは「寝る前に数枚」程度の短時間。

## 設計の原則

- **シンプルさが最優先。** 機能を足すより、1タップで音が鳴ることの気持ちよさを守る。
- **子どもが単独で操作できる。** 文字を読めない前提。タップ対象は大きく、
  誤操作で壊れる導線を作らない。設定画面のような「大人のUI」は表に出さない。
- **オフラインで完結する。** 移動中・機内でも動くこと。ネットワーク前提の機能を入れない。
- **広告・課金・外部送信は入れない。**

## 現状

`index.html` が単一ファイルの試作版。以下が動いている。

- ずかんモード: カード一覧 → タップで拡大表示、英語が自動再生。
  英語 / にほんご のボタンで聞き分け。聞いたカードには ⭐ が付く。
- クイズモード: 英語の音声が流れ、3枚から選ぶ。正解で紙吹雪。
- 音声は Web Speech API（`speechSynthesis`）。**これは試作用。下記の通り差し替える。**
- カードデータは `CARDS` 配列にベタ書き。絵柄は絵文字のプレースホルダ。

## 次にやること（優先順）

### 1. 音声を事前生成の mp3 に差し替える

Web Speech API は iOS で声の種類が端末依存になり、日本語と英語で声質が揃わない、
あるいは鳴らないことがある。オフラインでも保証できない。

- `scripts/generate-audio.mjs` を作り、`cards.json` を読んで英日の mp3 を一括生成する。
- TTS は OpenAI TTS か Google Cloud TTS。英日で同じエンジンに揃えて声のトーンを統一する。
- 生成物は `public/audio/en/{id}.mp3`, `public/audio/ja/{id}.mp3` に出力。
- API キーは `.env` に置き、`.gitignore` に入れる。生成済み mp3 はコミットする
  （数十枚なら容量的に問題なく、ビルド環境にキーが不要になる）。
- 再生は `<audio>` か Web Audio。iOS の自動再生制限があるので、
  最初のユーザー操作で無音バッファを1回鳴らして unlock する処理を入れる。

### 2. データと描画を分離する

カードを増やす作業が「`cards.json` に1行足して、画像と音声を置く」で終わる形にする。

```json
{ "id": "apple", "en": "apple", "ja": "りんご",
  "image": "/images/apple.webp", "tint": "red", "category": "food" }
```

音声パスは id から導出する（`/audio/en/${id}.mp3`）。JSON に書かせない。

### 3. PWA 化

- Vite + `vite-plugin-pwa`。
- Service Worker で画像・音声を precache し、完全オフライン動作にする。
- `display: standalone`、アイコン、スプラッシュを用意。
- Wake Lock API で、聞いている間に画面が暗くならないようにする。

### 4. 絵柄の差し替え

現在は絵文字。画像に置き換える。方針は未定（イラスト起こし / 写真 / 生成）。
どの方針でも、`image` フィールドの差し替えだけで済むようにしておく。

## 保留にしている機能

作者と相談して「入れない、または後回し」と判断したもの。勝手に足さないこと。

- **まねっこ録音**（英語のあとに自分の声を録って聞き比べる）:
  食いつきは強いが、マイク許可まわりで複雑になるため様子見。
- **ガチャ / シャッフル**（1枚だけランダム表示）: モードが増えすぎるため保留。
- 学習進捗のグラフ、レベル、連続日数などのゲーミフィケーション: 入れない。

## 技術メモ

- ビルドは Vite（vanilla、フレームワークなし）。React は現状不要。
- 状態は試作では `known` という `Set` のインメモリ。永続化するなら `localStorage` で十分。
- `prefers-reduced-motion` を尊重すること（試作の CSS に既に入っている）。
- タップ対象は最低 44px。`user-select: none` と `-webkit-tap-highlight-color: transparent`
  を維持し、長押しでテキスト選択やコンテキストメニューが出ないようにする。

## 実機確認

iPad / iPhone の Safari で確認する。`vite --host` で LAN に公開するか、
Tailscale 経由で開発機の IP を叩く。音声とタップの感触は実機でしか判断できないので、
音まわりを触ったら必ず実機で鳴らすこと。
