# おしゃべりカード（こども英語カードアプリ）

## これは何か

未就学児向けの、絵カードをタップすると英語と日本語の発音が鳴るアプリ。
市販の「音の出る英語カード」玩具のデジタル版。**iPad mini** のホーム画面から
PWA として起動して使うことを想定している（レイアウトは iPad mini を基準に組む）。

公開先: https://kidapu.github.io/Kids-Word-Card/

作者の子どもが実際に使う。想定利用シーンは「寝る前に数枚」程度の短時間。

## 設計の原則

- **シンプルさが最優先。** 機能を足すより、1タップで音が鳴ることの気持ちよさを守る。
- **子どもが単独で操作できる。** 文字を読めない前提。タップ対象は大きく、
  誤操作で壊れる導線を作らない。設定画面のような「大人のUI」は表に出さない。
- **オフラインで完結する。** 移動中・機内でも動くこと。ネットワーク前提の機能を入れない。
- **広告・課金・外部送信は入れない。**

## 現状

Vite + React 19 + Tailwind CSS v4。`main` への push で GitHub Pages に自動デプロイされる。

- ヘッダの **ABC / あいう** で「先に鳴らすことば」を切り替える（ずかん・クイズ共通）。
  あいう を選ぶと日本語が主になり、カードの大きい文字・自動再生・クイズの出題が
  すべて日本語になる。起動時は必ず ABC（英語から）に戻る。
- ずかんモード: カード一覧 → タップで拡大表示。開くと2つのことばが
  **続けて自動再生**される（英語モードなら英語→日本語、日本語モードなら逆）。
  ボタンは **🔊 もういっかい** の1つだけで、押すと同じ順でもう一度鳴る。
  下まで行くと次の巡りが継ぎ足されて、いくらでもスクロールできる
  （`MAX_PAGES` = 30 巡りで止まる。際限なく DOM が増えないための歯止め）。
  **🔀 シャッフル** で混ぜ直し、**🗂 カテゴリ** でカテゴリ順に戻る。
  どちらも先頭から配り直し、継ぎ足される巡りもその並び方に従う。
- クイズモード: 音声が流れ、3枚から選ぶ。正解でピンポンと紙吹雪、間違えたらブー。
  カードの絵の下に出すのは**出題したことば**（英語モードなら `apple`、日本語モードなら
  「りんご」）。もう一方のことばは出さない。ここを逆にしないこと。
  正解すると、**出題とは逆のことば → 出題したことば** の順に両方読む
  （英語で出したら「にんじん」→ `carrot`、日本語で出したらその逆）。
  読み終わってから次の問題に進むので、長いことばでも切れない。
- 効果音は音声ファイルを持たず `src/sfx.js` で Web Audio 合成している。
  上のメニューを押したときの「ピン」も同じ仕組み（押した反応が分かるように鳴らす）。
- カードは `src/cards.js` に 88 枚。5つのカテゴリ（`cat`）で色分けしている。
- 音声は Web Speech API（`speechSynthesis`）を `useSpeech` フックに閉じ込めてある。
  **これは暫定。下記の通り mp3 に差し替える。**
- 絵柄は絵文字のプレースホルダ。

ファイルの置き場所は README.md に一覧がある。

## 次にやること（優先順）

### 1. 音声を事前生成の mp3 に差し替える

Web Speech API は iOS で声の種類が端末依存になり、日本語と英語で声質が揃わない、
あるいは鳴らないことがある。オフラインでも保証できない。

- `scripts/generate-audio.mjs` を作り、`cards.json` を読んで英日の mp3 を一括生成する。
- TTS は OpenAI TTS か Google Cloud TTS。英日で同じエンジンに揃えて声のトーンを統一する。
- 生成物は `public/audio/en/{id}.mp3`, `public/audio/ja/{id}.mp3` に出力。
- API キーは `.env` に置き、`.gitignore` に入れる。生成済み mp3 はコミットする
  （数十枚なら容量的に問題なく、ビルド環境にキーが不要になる）。
- 再生は `<audio>` か Web Audio。iOS の自動再生制限があるので、
  最初のユーザー操作で無音バッファを1回鳴らして unlock する処理を入れる。

### 2. データを JSON に出す

`src/cards.js` に分離済み。次は JSON にして、画像と音声のパスを持たせる。
カードを増やす作業が「1行足して、画像と音声を置く」で終わる形を保つこと。

```json
{ "id": "apple", "en": "apple", "ja": "りんご",
  "image": "/images/apple.webp", "tint": "red", "category": "food" }
```

音声パスは id から導出する（`/audio/en/${id}.mp3`）。JSON に書かせない。

### 3. PWA 化

- `vite-plugin-pwa` を入れる。
- Service Worker で画像・音声を precache し、完全オフライン動作にする。
- `display: standalone`、アイコン、スプラッシュを用意。
- Wake Lock API で、聞いている間に画面が暗くならないようにする。

### 4. 絵柄の差し替え

現在は絵文字。画像に置き換える。方針は未定（イラスト起こし / 写真 / 生成）。
どの方針でも、`image` フィールドの差し替えだけで済むようにしておく。

## 保留にしている機能

作者と相談して「入れない、または後回し」と判断したもの。勝手に足さないこと。

- **まねっこ録音**（英語のあとに自分の声を録って聞き比べる）:
  食いつきは強いが、マイク許可まわりで複雑になるため様子見。
- **ガチャ**（1枚だけランダム表示）: モードが増えすぎるため保留。
  一覧の並びを入れ替える 🔀 シャッフルは、モードを増やさないので実装済み。
- 学習進捗のグラフ、レベル、連続日数などのゲーミフィケーション: 入れない。

## 技術メモ

- Vite + React 19 + Tailwind CSS v4。Tailwind は `@tailwindcss/vite` プラグイン方式で、
  設定ファイルは持たず `src/index.css` の `@theme` に色とブレークポイントを書いている。
- ブレークポイントは iPad mini 基準。縦(744px)で 4 列、横(1133px)で 5 列になるよう
  `--breakpoint-md` を 44rem に下げてある。数字を触るときはこの前提を壊さないこと。
- `vite.config.js` の `base` は Pages のパス。ここを変えると公開先で 404 になる。
- `prefers-reduced-motion` を尊重すること（`src/index.css` に既に入っている）。
- タップ対象は最低 44px。`user-select: none` と `-webkit-tap-highlight-color: transparent`
  を維持し、長押しでテキスト選択やコンテキストメニューが出ないようにする。

## 実機確認

iPad mini の Safari で確認する。`npm run dev -- --host` で LAN に公開するか、
Tailscale 経由で開発機の IP を叩く。音声とタップの感触は実機でしか判断できないので、
音まわりを触ったら必ず実機で鳴らすこと。
